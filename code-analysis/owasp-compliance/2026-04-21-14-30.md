# OWASP ASI Compliance Report

**Project:** Anamnesis — Medical Symptom Explorer  
**Generated:** 2026-04-21-14-30  
**Scope:** Full repository (`c:\Dev\anamnesis`)  
**Agent Type:** Single-agent LLM-powered medical symptom conversation system  
**Agent Framework:** Custom .NET 10 Blazor WebAssembly/Server with Ollama (MedGemma 27B) integration  

---

## Executive Summary

Anamnesis is a proof-of-concept medical symptom exploration application built on .NET 10 with a Blazor frontend and an Ollama-based LLM adapter. The system uses Google DeepMind's MedGemma 27B model to conduct conversational symptom intake via a chat interface.

**Overall Score: 1/10 Controls Covered**

The application is a single-user, local-first application with no authentication, no multi-agent communication, and no privilege model. While this reduces the attack surface significantly, it also means most ASI controls are not applicable. The primary security concerns center around prompt injection, lack of input validation, and absence of audit logging.

---

## Compliance Summary

| # | Risk | Status | Finding |
|---|------|--------|---------|
| ASI-01 | Prompt Injection | **FAIL** | No input validation before LLM calls; safety relies solely on system prompt |
| ASI-02 | Insecure Tool Use | **FAIL** | No tool allowlist; user input flows directly into LLM messages without sanitization |
| ASI-03 | Excessive Agency | **FAIL** | No capability boundaries; unbounded conversation history; no scope limits |
| ASI-04 | Unauthorized Escalation | **FAIL** | No privilege model exists in the application |
| ASI-05 | Trust Boundary Violation | **N/A** | Single-agent system; no inter-agent communication |
| ASI-06 | Insufficient Logging | **FAIL** | No structured audit trail for LLM tool calls; only standard ASP.NET Core logging |
| ASI-07 | Insecure Identity | **N/A** | Single-user local application; no multi-agent identity required |
| ASI-08 | Policy Bypass | **FAIL** | Safety policy is LLM-based (system prompt), not deterministic enforcement |
| ASI-09 | Supply Chain Integrity | **FAIL** | No dependency pinning, SBOM, or integrity manifests |
| ASI-10 | Behavioral Anomaly | **FAIL** | No circuit breakers, kill switch, or anomaly detection |

---

## Detailed Findings

### ASI-01: Prompt Injection — FAIL

**Risk:** User input is not validated or sanitized before being sent to the LLM. The system relies entirely on the system prompt's safety guidelines to prevent malicious input from causing harmful output.

**Evidence:**

- `ConversationService.SendAsync()` (line 23-35 of `ConversationService.cs`) adds user messages directly to the conversation history without any sanitization:
  ```csharp
  _history.Add(new ConversationMessage("user", userMessage));
  var response = await _ollamaClient.ChatAsync(_history);
  ```

- `Home.razor` accepts raw user input via `MudTextField` with no server-side validation beyond non-empty checks:
  ```csharp
  if (string.IsNullOrWhiteSpace(_symptomInput))
  {
      _showValidationError = true;
      return;
  }
  ```

- No input validation patterns found: no `validate_input`, `sanitize`, `classify_intent`, or `threat_detect` logic anywhere in the codebase.

- No negative patterns (eval, exec, subprocess) found — the application does not execute user input as code.

**What passing looks like:**
```csharp
// GOOD: Validate before tool execution
var validated = inputValidator.Sanitize(userMessage);
if (validated.IsMalicious)
    return "Request blocked by policy";
var response = await _ollamaClient.ChatAsync(validated.CleanedInput);
```

**Recommendation:**
- Add input length limits (e.g., max 10,000 characters)
- Implement content classification to detect jailbreak attempts or malicious prompts
- Add output filtering to prevent the LLM from generating harmful content
- Consider a deterministic policy layer (e.g., keyword filtering) before LLM calls

---

### ASI-02: Insecure Tool Use — FAIL

**Risk:** The only tool in this system is the Ollama HTTP API. There is no tool allowlist, no argument validation, and user-controlled data flows directly into the API request body.

**Evidence:**

- `OllamaClient.ChatAsync()` (line 19-42 of `OllamaClient.cs`) constructs an HTTP POST with user messages directly in the request body:
  ```csharp
  var request = new OllamaChatRequestDto(
      Model: _settings.Model,
      Messages: messages.Select(m => new OllamaMessageDto(m.Role, m.Content)),
      Stream: false
  );
  var response = await _httpClient.PostAsJsonAsync("/api/chat", request);
  ```

- No tool allowlist mechanism exists anywhere in the codebase.

- No argument validation on the HTTP request — all message content is passed through as-is.

**Recommendation:**
- Implement a tool allowlist even for single-tool systems (explicitly declare allowed tools)
- Add argument validation (input length, character restrictions)
- Consider rate limiting on the HTTP client to prevent abuse
- Add request/response size limits to prevent denial-of-service

---

### ASI-03: Excessive Agency — FAIL

**Risk:** The agent has no explicit capability boundaries. The conversation history grows unbounded, and there are no scope limits on what the agent can do.

**Evidence:**

- `ConversationService._history` is an unbounded `List<ConversationMessage>` that grows indefinitely:
  ```csharp
  private readonly List<ConversationMessage> _history = [];
  ```

- No capability list or execution ring mechanism exists.

- No principle of least privilege applied to tool access.

**Recommendation:**
- Implement conversation history truncation (e.g., keep only last N messages)
- Define explicit capability boundaries (e.g., "this agent can only conduct symptom interviews")
- Add session timeout and maximum conversation length limits
- Implement a kill switch to terminate conversations

---

### ASI-04: Unauthorized Escalation — FAIL

**Risk:** The application has no privilege model. While this is a local single-user application (reducing risk), there is no mechanism to prevent privilege escalation if the application were extended to support multiple users.

**Evidence:**

- No authentication or authorization code exists anywhere in the codebase.
- No privilege level checks in any component.
- No self-promotion patterns detected (the agent cannot modify its own configuration).

**Recommendation:**
- If multi-user support is added in the future, implement authentication (e.g., ASP.NET Identity)
- Add role-based access control for any administrative features
- Ensure the agent cannot modify its own system prompt or configuration

---

### ASI-05: Trust Boundary Violation — N/A

**Assessment:** This is a single-agent system with no inter-agent communication. There are no trust boundaries to enforce between agents. This control is not applicable.

**Recommendation:** If the system is extended to support multiple agents (e.g., a triage agent delegating to a symptom explorer), implement:
- Agent identity verification (DIDs or signed tokens)
- Trust score checks before accepting delegated tasks
- Delegation narrowing (child scope <= parent scope)

---

### ASI-06: Insufficient Logging — FAIL

**Risk:** There is no structured audit trail for LLM tool calls. The application uses only standard ASP.NET Core logging with minimal configuration.

**Evidence:**

- `appsettings.json` configures logging at a high level only:
  ```json
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  ```

- No structured logging for LLM API calls (no audit entries with timestamp, agent ID, tool name, args, result, policy decision).

- No tamper-evident logging (no hash-chained or append-only log format).

- Logs are not exported to secure storage.

**Recommendation:**
- Implement structured JSONL audit logging for all LLM interactions
- Include: timestamp, session ID, input length, output length, policy decision
- Store logs in a separate, tamper-evident storage (e.g., Azure Blob Storage with immutability policy)
- Add log rotation and retention policies

---

### ASI-07: Insecure Identity — N/A

**Assessment:** This is a single-user local application with no authentication. There is no multi-agent identity to manage. This control is not applicable.

**Recommendation:** If the system is extended to support multiple users or agents:
- Implement cryptographic agent identity (DIDs or signed tokens)
- Use per-agent credentials with rotation
- Bind identity to specific capabilities

---

### ASI-08: Policy Bypass — FAIL

**Risk:** Safety policy enforcement is entirely LLM-based (via the system prompt). There is no deterministic policy engine to prevent the agent from bypassing its constraints.

**Evidence:**

- `PromptTemplates.MedGemmaSystemPrompt` contains the safety guidelines:
  ```csharp
  public const string MedGemmaSystemPrompt = """
      ...
      IMPORTANT GUIDELINES:
      - Do NOT provide diagnoses, medical advice, or treatment recommendations
      - Do NOT speculate about conditions or suggest what the user might have
      - If the user asks for a diagnosis, politely decline and redirect to symptom exploration
      ...
      """;
  ```

- No deterministic policy engine exists. The system prompt is the only enforcement mechanism.
- No fail-closed behavior — if the LLM ignores the prompt, there is no fallback denial.

**What passing looks like:**
```csharp
// GOOD: Deterministic policy enforcement
var policyResult = policyEvaluator.Evaluate(userInput);
if (policyResult.Action == PolicyAction.Deny)
    return "Request blocked by policy";
var response = await _ollamaClient.ChatAsync(userInput);
```

**Recommendation:**
- Implement a deterministic policy layer (e.g., keyword filtering, regex-based detection) before LLM calls
- Add output validation to detect and block harmful LLM responses
- Ensure policy checks cannot be bypassed or overridden by the agent
- Implement fail-closed behavior (if policy check errors, deny the action)

---

### ASI-09: Supply Chain Integrity — FAIL

**Risk:** The application has no supply chain integrity mechanisms. Dependencies are managed via NuGet without integrity verification.

**Evidence:**

- `Anamnesis.Interface.Website.csproj` references NuGet packages without integrity manifests:
  ```xml
  <PackageReference Include="MudBlazor" Version="9.3.0" />
  ```

- No `INTEGRITY.json` or manifest files with SHA-256 hashes.
- No SBOM (Software Bill of Materials) generation.
- No dependency pinning verification.

**Recommendation:**
- Generate an SBOM for the project (e.g., using `dotnet-sbom`)
- Implement dependency pinning with upper bounds (e.g., `>= 9.3.0 < 10.0.0`)
- Add integrity verification for all dependencies
- Consider using a private NuGet feed with signed packages

---

### ASI-10: Behavioral Anomaly — FAIL

**Risk:** The system has no mechanism to detect or respond to agent behavioral drift or anomalous behavior.

**Evidence:**

- No circuit breakers for repeated failures.
- No trust score decay over time.
- No kill switch or emergency stop capability.
- No anomaly detection on tool call patterns (frequency, targets, timing).

**What passing looks like:**
```python
# GOOD: Circuit breaker and kill switch
if failureCount >= CIRCUIT_BREAKER_THRESHOLD:
    trip_circuit_breaker()
    return "Service temporarily unavailable"

if trustScore < TRUST_THRESHOLD:
    trigger_kill_switch()
    return "Session terminated due to anomalous behavior"
```

**Recommendation:**
- Implement circuit breakers for LLM API failures
- Add session timeout (e.g., 30 minutes of inactivity)
- Implement a kill switch to terminate conversations
- Add anomaly detection on conversation patterns (e.g., rapid-fire requests, unusual input patterns)

---

## Critical Gaps

| Priority | Gap | Impact |
|----------|-----|--------|
| **CRITICAL** | ASI-01: No input validation before LLM calls | User could inject malicious prompts to bypass safety guidelines |
| **CRITICAL** | ASI-08: Policy enforcement is LLM-based, not deterministic | Agent can be prompted to ignore its own safety constraints |
| **HIGH** | ASI-02: No tool allowlist or argument validation | Unrestricted input flows into the LLM API |
| **HIGH** | ASI-03: Unbounded conversation history | Potential memory exhaustion; context window overflow |
| **HIGH** | ASI-06: No audit trail for LLM interactions | No ability to investigate incidents or track agent behavior |
| **MEDIUM** | ASI-09: No supply chain integrity | Vulnerable to dependency supply chain attacks |
| **MEDIUM** | ASI-10: No behavioral monitoring | Cannot detect or respond to anomalous agent behavior |

---

## Recommendations Summary

### Immediate (Critical)
1. **Add input validation** before LLM calls — length limits, content classification, and keyword filtering
2. **Implement deterministic policy enforcement** — a non-LLM layer that validates inputs/outputs against safety rules
3. **Add output filtering** to detect and block harmful LLM responses

### Short-term (High Priority)
4. **Implement conversation history truncation** — keep only the last N messages to prevent memory exhaustion
5. **Add structured audit logging** for all LLM interactions
6. **Implement a kill switch** to terminate conversations

### Medium-term
7. **Add session timeout** and rate limiting
8. **Generate an SBOM** for dependency tracking
9. **Implement dependency pinning** with upper bounds

### Long-term (If scaling to multi-user)
10. **Add authentication and authorization** (ASP.NET Identity)
11. **Implement agent identity verification** if multi-agent support is added
12. **Deploy logs to secure storage** with immutability policies

---

## Architecture Notes

The Anamnesis application follows a clean architecture pattern:

```
Anamnesis.Interface.Website (Blazor UI)
  └── Anamnesis.UseCase.Conversation (ConversationService)
        └── Anamnesis.Adapter.Ollama (OllamaClient)
              └── Anamnesis.Domain (ConversationMessage)
```

This layered architecture is a positive security practice as it separates concerns and limits the blast radius of any single component. However, the security controls that should exist at each layer are currently absent.

---

## Conclusion

Anamnesis is a proof-of-concept application with a relatively small attack surface due to its single-user, local-first design. However, the lack of input validation, deterministic policy enforcement, and audit logging represents significant security gaps that must be addressed before this application could be considered for any production or multi-user deployment.

The most critical finding is that **safety is entirely dependent on the LLM's system prompt** — if a user can craft a prompt that causes the LLM to ignore its safety guidelines, there is no fallback mechanism to prevent harmful output. This is a well-known vulnerability in LLM-based systems and must be addressed with deterministic guardrails.

**Final Score: 1/10 Controls Covered (N/A controls excluded)**
